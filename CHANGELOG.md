# Changelog

## [alpha1.1.0] - 1 July 2026

- Completely overhauled handlers and resolutions, moving them to `src/siege/core/_handlers.py`.
- Replaced old errors with `fail`, `assert_...` and `cancel_action`.
- Added `DebugAPI` and a module entrypoint (`src/siege/__main__.py`)
